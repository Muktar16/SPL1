#include<iostream>
#include<stdlib.h>
#include<stdio.h>
using namespace std;


void printFrequencyPolygon(double *LB,double* UB,int *F,int n)
{
    system("cls||clear");
    double MidPoint[n];
    for(int i=0;i<n;i++)
    {
        MidPoint[i]=(LB[i]+UB[i])/2;
    }

    for(int i=0;i<n;i++)
    {
        //printf("\t%d |",);
    }
}
